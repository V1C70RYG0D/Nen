apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus-test:9090
    isDefault: true
    jsonData:
      tlsSkipVerify: true
      timeInterval: 5s

